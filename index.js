/**
 * Mongoose Mailer
 * @description Handle blog posts and send emails to subscribers
 * @author Joshua Grimmett
 */

require('dotenv').config();

// Import app
const app = require('./src/app');

// Start server
app.startServer();