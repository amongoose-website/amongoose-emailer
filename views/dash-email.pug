extends ./themes/dash.pug

block head
    title #{pageTitle} | Mongoose Mailer

block content
    h5 Status
    if !email.posted
        p This post is #[a(href=`https://amongoose.com/posts/${email.fileName}`) #[button.btn.btn-outline-success.btn-sm Live]]

        if email.sentNotifications < 
    else
        p This post is not live. #[a(href=`${email.fileName}/goLive`) #[button.btn.btn-primary.btn-sm Go live]]
    h5 Content
    div.border.rounded.p-2.text-center!=post.html
    pre=JSON.stringify(email.parsedEmail.attachments, null, 4)